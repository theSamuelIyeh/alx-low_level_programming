#include <stdio.h>

/**
 * main - entry point
 *
 * Description: print all lower case alphabet
 *
 * Return 0
 */
int main(void)
{
	char letter = 'a';

	while (ch <= z)
	{
		putchar("%c", letter);
		letter++;
	}
	return (0);
}
